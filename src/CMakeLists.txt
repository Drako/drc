FILE ( GLOB drc_SOURCES *.cxx )
FILE ( GLOB drc_HEADERS *.hxx )

INCLUDE_DIRECTORIES ( ${Boost_INCLUDE_DIRS} )
LINK_DIRECTORIES ( ${Boost_LIBRARY_DIRS} )

ADD_EXECUTABLE ( drc ${drc_SOURCES} ${drc_HEADERS} )
TARGET_LINK_LIBRARIES ( drc ${Boost_LIBRARIES} )

INSTALL ( TARGETS drc DESTINATION bin )

